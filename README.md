# 🪄 ABRA — Умный переключатель раскладки

![Abra Icon](main_icon.ico)

**ABRA** — это легкая и надежная утилита для Windows, которая исправляет текст, набранный в неверной раскладке (например: `Ghbdtn` → `Привет`).

В отличие от аналогов (Punto Switcher, Caramba), Abra создана с упором на **полный контроль**: она не переключает раскладку сама, когда вы этого не хотите, и работает через низкоуровневые команды драйвера, что позволяет ей исправлять текст даже в "капризных" приложениях (Adobe Premiere, After Effects, игры).

## 🚀 Почему Abra?

* **🛡 Безопасность:** Полностью открытый исходный код. Никакой слежки, телеметрии и отправки ваших данных на сервера.
* **🧠 Умный контекст:** Программа анализирует весь текст. Если вы написали код или технический термин (напр. `Render`), она поймет это и не будет переводить его в бессмыслицу.
* **💪 Работает везде:** Использует метод `Direct Input` (скан-коды) и `Shift+Insert` для вставки. Это значит, что текст исправится там, где обычные переключатели не срабатывают.
* **✨ Умная пунктуация:** Автоматически исправляет частые опечатки, возникающие при быстрой печати (например, `ююю` → `...` или `j,` → `об`).
* **☁️ Синхронизация:** Умеет подтягивать словарь исключений из Google Таблиц (полезно для команд или работы на разных ПК).

## 📥 Установка для пользователей

1. Перейдите в раздел [Releases](ссылка-на-твои-релизы-на-гитхабе) (справа).
2. Скачайте архив `Abra.zip`.
3. Распакуйте в удобную папку.
4. Запустите `Abra.exe`.

Программа свернется в трей (синий значок с пером).

## 🎮 Как пользоваться

### Основные команды
* **`Ctrl + Alt + K`** (или `Л` на русской раскладке) — **Исправить выделенный текст**.
    * *Выделили кракозябры -> Нажали -> Получили текст.*
* **`Ctrl + Alt + A`** (или `Ф` на русской раскладке) — **Добавить слово в исключения**.
    * *Если программа пытается перевести слово `Dota` в `Вщеф`, выделите его и нажмите это сочетание. Больше оно переводиться не будет.*

### Настройки
Кликните правой кнопкой мыши по иконке в трее и выберите **Настройки**:
* Можно изменить горячую клавишу (достаточно ввести одну букву, например `Z`, и сочетание станет `Ctrl+Alt+Z`).
* Включить/выключить уведомления.
* Отредактировать файл исключений вручную.

## 🛠 Запуск из исходного кода (для разработчиков)

Если вы хотите доработать Abra или собрать её самостоятельно:

1. **Клонируйте репозиторий:**
   ```bash
   git clone [https://github.com/ВАШ_НИК/Abra.git](https://github.com/ВАШ_НИК/Abra.git)
   cd Abra

2. **Установите зависимости:**
   pip install -r requirements.txt

3. **Запустите:**
   python smart_layout_v21_final.pyw

4. **Сборка в .exe:**
    pyinstaller --noconsole --onefile --clean --icon=main_icon.ico --name=Abra --collect-all spellchecker smart_layout_v21_final.pyw

## 🤝 Поддержка автора

Этот проект разрабатывается в свободное время. Если Abra сэкономила вам нервы и время, вы можете угостить автора кофе ☕:
https://pay.cloudtips.ru/p/dfa5bb67
# Abra
Легкий и умный переключатель раскладки клавиатуры для Windows (альтернатива Punto Switcher). Open Source.
